#! /usr/bin/env perl

use strictures;

use RPC::Serialized::Server::NetServer;
use Try::Tiny;
use UUID::Tiny;

exit sciolist_agent(@ARGV);

sub sciolist_agent
{
	RPC::Serialized::Server::NetServer->new({
		net_server => {
			port => '/tmp/sciolist-agent.sock|unix',
		},
	})->run;
	return 1;
}
